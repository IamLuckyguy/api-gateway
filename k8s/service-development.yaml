apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: kwt-development
spec:
  selector:
    project: kwt
    app: gateway
    env: development
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
